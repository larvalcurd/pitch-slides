export const SLIDE_WIDTH = 960;
export const DEFAULT_TEXT_X = 20;
export const DEFAULT_TEXT_Y = 20;
export const DEFAULT_TEXT_WIDTH = 300;
export const DEFAULT_TEXT_HEIGHT = 80;
export const DEFAULT_IMAGE_WIDTH = 240;
export const DEFAULT_IMAGE_HEIGHT = 160;
export const DEFAULT_IMAGE_PADDING = 30;

export const DEFAULT_MIN_WIDTH = 20;
export const DEFAULT_MIN_HEIGHT = 20;

export const MAX_TEXT_LENGTH = 500;
